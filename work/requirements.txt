# Privacy News Monitor - Dependencies
# 이 프로젝트는 Python 표준 라이브러리만 사용합니다.
# 추가적인 의존성이 필요하지 않습니다.

# 선택적 의존성 (향후 확장용):
requests>=2.28.0  # HTTP 요청 (현재 urllib 사용)
beautifulsoup4>=4.11.0  # 웹 스크래핑 확장
python-dateutil>=2.8.0  # 날짜 파싱 확장
streamlit>=1.28
openai>=1.0.0
python-dotenv>=1.0.0
reportlab